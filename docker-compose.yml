version: '3.8'

services:
  multiplication-bot:
    build: .
    container_name: multiplication-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data:rw
    environment:
      - DB_PATH=/app/data/multiplication_game.db